const CLIENT = {
    LOGIN: 'LOGIN',
    SETTING: 'SETTING',
    SCHEDULE: 'SCHEDULE',
    SCHEDULEDRAWER: 'SCHEDULEDRAWER',
};

const API = {
    LOGIN: '/login',
    SETTING: '/settings',
    SCHEDULE: '/SCHEDULE',
};

export { CLIENT, API };
