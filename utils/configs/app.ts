import { API_URL, APP_NAME } from '@env';

console.info('API Base Url: ', API_URL || '');
console.info('App Name: ', APP_NAME || 'quanlybaotri');
export default {
    API_URL: API_URL || '',
    APP_NAME: APP_NAME || 'quanlybaotri',
};
